-- Create the database if it doesn't exist
CREATE DATABASE IF NOT EXISTS airtel_project;

-- Use the created database
USE airtel_project;

-- Set character set to utf8mb4 for better compatibility
ALTER DATABASE airtel_project CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
